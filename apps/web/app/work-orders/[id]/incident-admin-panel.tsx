'use client';
import {useCallback,useEffect,useState} from 'react';import {createClient} from '../../../lib/supabase/client';import {Incident,incidents,reviewIncident} from '../../../lib/work-order-incident-api';
export function IncidentAdminPanel({workOrderId}:{workOrderId:string}){const [items,setItems]=useState<Incident[]>([]),[message,setMessage]=useState('');const load=useCallback(async()=>{const {data}=await createClient().auth.getSession();if(data.session)setItems(await incidents(workOrderId,data.session.access_token));},[workOrderId]);useEffect(()=>{void load()},[load]);async function act(item:Incident,action:'ACKNOWLEDGE'|'RESOLVE'|'REOPEN',resolution?:string){try{const {data}=await createClient().auth.getSession();if(!data.session)return;await reviewIncident(workOrderId,item.id,data.session.access_token,{operationId:crypto.randomUUID(),action,resolution});await load();setMessage('Incident review recorded.');}catch(e){setMessage(e instanceof Error?e.message:'Review failed.')}}if(!items.length)return null;return <article id="incidents" className="executionChecklist"><h2>Field incidents</h2><p>Original reports and evidence remain unchanged. Resolution records operational follow-up only.</p>{items.map(item=><section key={item.id}><h3>{item.category.replaceAll('_',' ')}</h3><p>{item.note}</p><p className="muted">{item.technician.firstName} {item.technician.lastName} · {new Date(item.fieldReportedAt).toLocaleString()}{item.section?` · ${item.section.title}`:''} · {item.status.toLowerCase()}</p>{item.evidence.length?<p>{item.evidence.length} linked evidence item(s) · private authorized access only</p>:null}{item.reviews.map(r=><p key={r.id} className="muted">{new Date(r.createdAt).toLocaleString()} · {r.actor.firstName} {r.actor.lastName} · {r.action.toLowerCase()}{r.resolution?` · ${r.resolution.replaceAll('_',' ').toLowerCase()}`:''}</p>)}{item.status==='OPEN'?<button onClick={()=>void act(item,'ACKNOWLEDGE')}>Acknowledge</button>:null}{item.status!=='RESOLVED'?<label>Resolve operationally<select defaultValue="" onChange={e=>e.target.value&&void act(item,'RESOLVE',e.target.value)}><option value="">Choose action</option><option value="NO_FURTHER_OPERATIONAL_ACTION">No further operational action</option><option value="FOLLOW_UP_COMPLETED">Follow-up completed</option><option value="ESCALATED_OUTSIDE_WORKFLOW">Escalated outside this workflow</option></select></label>:<button onClick={()=>void act(item,'REOPEN')}>Reopen for review</button>}</section>)}{message?<p className="syncNotice">{message}</p>:null}</article>}
