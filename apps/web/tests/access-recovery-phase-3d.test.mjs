import test from 'node:test';import assert from 'node:assert/strict';import {readFileSync} from 'node:fs';
const read=p=>readFileSync(new URL(p,import.meta.url),'utf8');
test('Phase 3D UI is Admin-scoped, channel-constrained, and never displays response contents',()=>{const page=read('../app/(authenticated)/work-orders/[id]/page.tsx'),panel=read('../app/work-orders/[id]/access-recovery-panel.tsx');assert.match(page,/appUser\.role === 'ADMIN'.*AccessRecoveryPanel/);assert.match(panel,/<select value=\{conversationId\}/);assert.match(panel,/WhatsApp/);assert.match(panel,/Facebook Messenger/);assert.match(panel,/Register for protected review/);assert.doesNotMatch(panel,/responseMessage\.(contentText|attachmentMetadata)/);assert.doesNotMatch(panel,/Cancel|Reschedule|Finance|Invoice|Payment/);});
