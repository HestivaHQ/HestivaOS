import test from 'node:test';import assert from 'node:assert/strict';import {readFileSync} from 'node:fs';const read=p=>readFileSync(new URL(p,import.meta.url),'utf8');
const panel=read('../app/work-orders/[id]/temporary-access-credentials-panel.tsx'),page=read('../app/work-orders/[id]/page.tsx'),api=read('../lib/api.ts');
test('protected credential panel is ADMIN-only and hides values by default',()=>{assert.match(page,/appUser\.role === 'ADMIN' \? <TemporaryAccessCredentialsPanel/);assert.match(panel,/type="password"/);assert.match(panel,/explicit, audited reveal/);assert.doesNotMatch(panel,/localStorage|sessionStorage/);});
test('credential lifecycle uses dedicated protected endpoints',()=>{for(const action of ['temporaryAccessCredentials','createTemporaryAccessCredential','reviewTemporaryAccessCredential','revealTemporaryAccessCredential','revokeTemporaryAccessCredential'])assert.match(api,new RegExp(action));assert.match(api,/revealTemporaryAccessCredential:[\s\S]*method:"POST"/);assert.doesNotMatch(api,/secretValue/);});
