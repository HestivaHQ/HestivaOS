import test from 'node:test';import assert from 'node:assert/strict';import{readFileSync}from'node:fs';const read=p=>readFileSync(new URL(`../${p}`,import.meta.url),'utf8');
test('management correction reuses Work Order completion review with scoped authority',()=>{const panel=read('app/work-orders/[id]/completion-correction-panel.tsx'),page=read('app/work-orders/[id]/page.tsx');assert.match(panel,/Authorize scoped correction/);assert.match(panel,/sectionIds/);assert.match(panel,/Original acknowledgement/);assert.match(page,/CompletionCorrectionPanel/);});
test('Technician correction resubmission uses the existing durable operations store',()=>{const store=read('app/technician/components/offline-store.ts'),brief=read('app/technician/components/job-brief.tsx'),api=read('lib/api.ts');assert.match(store,/kind:"RESUBMIT_CORRECTION"/);assert.match(store,/operations/);assert.match(brief,/activeCorrection/);assert.match(brief,/saveCorrectionResubmission/);assert.match(api,/completion-corrections\/\$\{correctionId\}\/resubmit/);});
