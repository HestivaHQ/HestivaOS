import test from 'node:test';import assert from 'node:assert/strict';import {readFileSync} from 'node:fs';const read=p=>readFileSync(new URL(`../${p}`,import.meta.url),'utf8');
test('management routes direct creation without legacy mode state',()=>{const manager=read('app/work-orders/work-orders-manager.tsx'),property=read('app/properties/properties-manager.tsx'),home=read('app/(authenticated)/page.tsx');assert.match(manager,/createRoute/);assert.match(property,/\/work-orders\/new\?customerId=/);assert.match(home,/href="\/work-orders\/new"/);assert.doesNotMatch(property,/mode=create/) });
test('Admin service scope UI preserves versions and controlled fields',()=>{const source=read('app/admin/settings/service-scopes/service-scope-manager.tsx');assert.match(source,/Create draft version/);assert.match(source,/Published and used versions remain historical/);assert.match(source,/ON_EXCEPTION/);assert.doesNotMatch(source,/deleteServiceScope|Delete version/) });
test('comparison is permission-aware and explicit',()=>{const page=read('app/(authenticated)/work-orders/[id]/page.tsx'),panel=read('app/work-orders/[id]/scope-revision-panel.tsx');assert.match(page,/appUser\.role === 'ADMIN'.*ScopeRevisionPanel/);assert.match(panel,/Compare scope/);assert.match(panel,/Adopt reviewed version/);assert.match(panel,/accepted Quote/);assert.match(panel,/Started scope is never changed/) });
