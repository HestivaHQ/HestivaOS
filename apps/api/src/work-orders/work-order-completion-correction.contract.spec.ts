import { describe,expect,it } from '@jest/globals';import {readFileSync} from 'node:fs';import {join} from 'node:path';
const read=(path:string)=>readFileSync(join(process.cwd(),path),'utf8');
describe('completion correction isolation contract',()=>{it('is additive and preserves frozen scope, evidence, incidents, interruptions and mismatch history',()=>{const migration=read('prisma/migrations/20260819210000_completion_corrections/migration.sql');expect(migration).toContain('work_order_completion_corrections');for(const table of ['work_order_execution_scope_revisions','execution_section_evidence','work_order_incidents','work_order_interruptions','work_order_scope_mismatch_resolutions'])expect(migration).not.toMatch(new RegExp(`(?:DELETE FROM|DROP TABLE|UPDATE) "?${table}`,'i'));});it('introduces no Finance or Customer Correspondence side effects',()=>{const source=read('src/work-orders/work-order-completion-correction.service.ts');expect(source).not.toMatch(/payment|refund|credit|price|whatsapp|messenger|email|send/i);expect(source).toContain('completionCorrespondenceEligibleAt:null');});});
