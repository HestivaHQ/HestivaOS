# Provenance

Material-change Correspondence provenance snapshots the source-event key, event type, operation and Work Order identities, material-change actor, lifecycle stage, commit timestamp, reason, override reason, previous snapshot, requested changes and recorded consequences, plus the ADMIN materializer identity.
