# Provisional COIDA costing input record — 2026-08-21

The operator verified that the current HestivaOS production environment uses `HESTIVA_COIDA_RATE=0.01`, which the quote costing model interprets as a 1% rate.

This is a temporary internal costing assumption only. It is not Hestiva's authoritative Compensation Fund assessed rate and must be replaced when the business receives or confirms its authoritative assessment.

The canonical Website Quote ingestion boundary records the same value and replacement requirement. This record changes no runtime environment variable, pricing formula, deployment configuration, Quote behavior, or other application behavior.
