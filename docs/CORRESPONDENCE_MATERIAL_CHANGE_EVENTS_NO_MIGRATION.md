# Schema impact

No database migration is required. The existing immutable `work_order_material_changes` table already provides the unique operation identity and persisted source facts required by this Correspondence integration.
